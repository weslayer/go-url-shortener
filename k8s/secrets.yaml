apiVersion: v1
kind: Secret
metadata:
  name: url-shortener-secrets
type: Opaque
data:
  redis-password: ${REDIS_PASSWORD_BASE64}
  backend-host: ${BACKEND_HOST_BASE64}
  cors-origin: ${CORS_ORIGIN_BASE64} 